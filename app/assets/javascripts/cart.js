$(document).ready(function(){
   // Only trigger if the element is fixed
   $('.cart-info-bar').width($('.container').width());
});

$(window).resize(function(){
   // Only trigger if the element is fixed
   $('.cart-info-bar').width($('.container').width());
});